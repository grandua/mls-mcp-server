{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'flowise start & node /app/dist/server.js'",
    "ports": [
      {
        "port": 3000,
        "protocol": "TCP"
      }
    ]
  }
}
